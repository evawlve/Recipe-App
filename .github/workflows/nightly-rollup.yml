name: Nightly Rollup (interactions)

on:
  schedule:
    - cron: '0 3 * * *'
  workflow_dispatch: {}

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Call rollup endpoint
        run: |
          set -euo pipefail
          curl -sS -X POST \
            -H "X-Cron-Secret: $CRON_SECRET" \
            "$VERCEL_URL/api/admin/cron/rollup"
        env:
          VERCEL_URL: ${{ secrets.VERCEL_URL }}
          CRON_SECRET: ${{ secrets.CRON_SECRET }}


